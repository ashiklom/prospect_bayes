#!/bin/bash
#
# Run this file using 'qsub multirun.qsub'
#
# All lines starting with "#$" are SGE qsub commands
# 

# Specify which shell to use
#$ -S /bin/bash

# Run on the current working directory
#$ -cwd

# Give this job a name
#$ -N "pbi_hre"

# Join standard output and error to a single file
#$ -j y

# Name the file where to redirect standard output and error
#$ -o prospbayes_log.qlog

# Send an email when the job begins and when it ends running
#$ -m be

# Whom to send the email to
#$ -M ashiklom@bu.edu

# Set walltime limit
#$ -l h_rt=96:00:00

# Run in parallel
#$ -pe mpi_16_tasks_per_node 16

# Now let's keep track of some information just in case anything goes wrong

cd ..
echo "=========================================================="
echo "Starting on : $(date)"
echo "Running on node : $(hostname)"
echo "Current directory : $(pwd)"
echo "Current job ID : $JOB_ID"
echo "Current job name : $JOB_NAME"
echo "Task index number : $SGE_TASK_ID"
echo "=========================================================="

# Run the R scripts
# Arguments to runpi.R: JumpRSD ngibbs runid folder

Rscript scripts/runpi.R 0.05 1e6 run01 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run02 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run03 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run04 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run05 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run06 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run07 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run08 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run09 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run10 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run11 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run12 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run13 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run14 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run15 $FOLDER &
Rscript scripts/runpi.R 0.05 1e6 run16 $FOLDER &

wait

echo "=========================================================="
echo "Finished on : $(date)"
echo "=========================================================="
